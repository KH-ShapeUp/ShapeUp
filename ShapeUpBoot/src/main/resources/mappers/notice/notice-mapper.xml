<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.ShapeUp.boot.domain.notice.model.mapper.NoticeMapper">

	<select id="getTotalCount" resultType="_int">
		SELECT COUNT(*)
		FROM NOTICE_TBL
		<where>
			<if test="category != null and category != ''">
				NOTICE_CATEGORY = #{category}
			</if>

			<if test="searchKeyword != null and searchKeyword != ''">
				AND
				<choose>
					<when test="searchType == 'T'.toString()">
						NOTICE_TITLE LIKE '%' || #{searchKeyword} || '%'
					</when>
					<when test="searchType == 'C'.toString()">
						NOTICE_CONTENT LIKE '%' || #{searchKeyword} || '%'
					</when>
					<when test="searchType == 'TC'.toString()">
						(NOTICE_TITLE LIKE '%' || #{searchKeyword} || '%' OR
						NOTICE_CONTENT LIKE '%' || #{searchKeyword} || '%')
					</when>
				</choose>
			</if>
		</where>
	</select>

<select id="selectNoticeList" resultType="Notice">
    SELECT NOTICE_NO, 
        NOTICE_TITLE, 
        NOTICE_CONTENT, 
        NOTICE_CATEGORY, 
        USER_NO, 
        CREATED_AT, 
        UPDATED_AT, 
        EVENT_START, 
        EVENT_END, 
        VIEW_COUNT
    FROM (
        SELECT ROWNUM RNUM, A.*
        FROM (
            SELECT NOTICE_NO, NOTICE_TITLE, NOTICE_CONTENT, NOTICE_CATEGORY, 
            USER_NO, CREATED_AT, UPDATED_AT, EVENT_START, EVENT_END, VIEW_COUNT
            FROM NOTICE_TBL
            <where>
                <if test="category != null and category != ''">
                    NOTICE_CATEGORY = #{category}
                </if>
                
                <if test="searchKeyword != null and searchKeyword != ''">
                	AND
                    <choose>
                        <when test='searchType != null and searchType.equals("T")'>
                            NOTICE_TITLE LIKE '%' || #{searchKeyword} || '%'
                        </when>
                        <when test='searchType != null and searchType.equals("C")'>
                            NOTICE_CONTENT LIKE '%' || #{searchKeyword} || '%'
                        </when>
                        <when test='searchType != null and searchType.equals("TC")'>
                            (NOTICE_TITLE LIKE '%' || #{searchKeyword} || '%' OR NOTICE_CONTENT LIKE '%' || #{searchKeyword} || '%')
                        </when>
                    </choose>
                </if>
            </where>
            ORDER BY CREATED_AT DESC
        ) A
    )
    WHERE RNUM BETWEEN #{startRow} AND #{endRow} 
</select>

	<insert id="insertNotice"
		parameterType="com.ShapeUp.boot.app.notice.dto.NoticeInsertDto">
		<selectKey keyProperty="noticeNo" resultType="_int"
			order="BEFORE">
			SELECT SEQ_NOTICE_NO.NEXTVAL FROM DUAL
		</selectKey>

		INSERT INTO NOTICE_TBL (
		NOTICE_NO, NOTICE_TITLE, NOTICE_CONTENT, NOTICE_CATEGORY,
        USER_NO, CREATED_AT, UPDATED_AT, EVENT_START, EVENT_END, VIEW_COUNT
		)
		VALUES(
		#{noticeNo}, #{noticeTitle}, #{noticeContent}, #{noticeCategory}
		, #{userNo}, SYSDATE, NULL, #{eventStart}, #{eventEnd}, 0
		)
	</insert>
	
	<update id="increaseViewCount">
		UPDATE NOTICE_TBL SET VIEW_COUNT = VIEW_COUNT + 1
		WHERE NOTICE_NO = #{noticeNo}
	</update>
	
	<select id="selectNoticeDetail" resultType="Notice">
		SELECT NOTICE_NO, NOTICE_TITLE, NOTICE_CONTENT, NOTICE_CATEGORY, USER_NO,
		CREATED_AT, UPDATED_AT, EVENT_START, EVENT_END, VIEW_COUNT
		FROM NOTICE_TBL
		WHERE NOTICE_NO = #{noticeNo}
	</select>
</mapper>