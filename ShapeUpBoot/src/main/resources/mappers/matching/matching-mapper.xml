<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.ShapeUp.boot.domain.matching.model.mapper.matchingMapper">
 	<insert id="matchingInsert">
 		INSERT INTO MATCHING VALUES (SEQ_MATCH_NO.NEXTVAL, 0, #{boardTitle}, #{boardContent}, DEFAULT, NULL, DEFAULT,
 		#{matchingLocation}, #{matchingDate}, #{matchingLevel}, #{matchingTime}, #{partnerType}, DEFAULT, #{matchingUserCount})
 	</insert>
 </mapper>